
<view>
  <view>
    <view class="tips">
        <label><text style="color : red">*</text>点击报修主题进入查看报修情况，完成或取消报修；</label>
    </view>
    <view class="tips">
        <label><text style="color : red">*</text>点击提交按钮，将报修申请发送给相关售后人员。</label>
    </view>
  </view>
  <view class="left">
    <view class="table">
      <view class="tr">
        <text class="th">报修主题</text>
        <text class="th">期待时间</text>
        <text class="th">提交报修</text>
      </view>
      <view class="tr" wx:for="{{array}}">
        <view class="td">
        <navigator url="../regstatus/regstatus?id={{item.party_id}}" style="color:blue">{{item.party_name}}</navigator>
        </view>
        <text class="td">{{item.party_time}}</text>
        <view class="td" style="color:blue">
        <navigator url="../register/register?id={{item.party_id}}">提交</navigator>
        </view>
      </view>
    </view>
  </view>
</view>