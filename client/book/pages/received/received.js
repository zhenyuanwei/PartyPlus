var utils = require('../../utils/util.js');
var app = getApp()
Page({
    data: {
    },
    //事件处理函数
    onLoad: function (options) {
        console.log('onLoad')
        var that = this
        //调用应用实例的方法获取全局数据

    }
})
